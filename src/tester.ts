/**
 * @fileoverview
 * This file is made to check sources that collected.
 * You probably won't need it.
 */
const data = require("./data/source.json");

console.log("Length of source.json ==", data.length);
